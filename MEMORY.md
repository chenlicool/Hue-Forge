# 2026-03-01 20:32
时间：2026-03-01 20:32
目标：提升 GitHub 仓库首页展示质量，使首次访问者能快速理解 Hue Forge 的定位与使用方式。
改动：影响面判断已完成；计划仅补充 README 首页结构与一张轻量封面图，不修改业务代码、构建配置和运行入口。
当前：仓库已完成初次推送，但首页说明仍偏简略，缺少首屏视觉锚点、使用流程说明与导出示例。
禁止动：不改 `src/` 逻辑、不引入额外构建依赖、不生成待确认/未知的产品承诺；仅优化文档展示面。
待办：新增 `docs/cover.svg`；更新 `README.md`；补充 `CHANGELOG.md`；提交并推送。
回滚：删除封面图并恢复当前 README 文案。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；展示内容必须与现有实现一致。

# 2026-03-01 20:13
时间：2026-03-01 20:13
目标：整理仓库文件结构，使项目以更清晰的目录形态进入 GitHub 托管。
改动：影响面判断已完成；计划仅归档根目录中的历史备份与辅助脚本，补充最小文档说明，并保持运行入口与源码目录不变。
当前：仓库尚未有首个提交；根目录同时存在运行文件、历史备份 `color-system-v2.0.zip`、辅助脚本 `calc.ts` 与遗留 `metadata.json`，公开托管可读性偏弱。
禁止动：不改 `src/` 业务逻辑、不调整 Vite 入口与依赖版本、不删除待确认/未知但仍需保留的备份文件内容；仅做归档整理。
待办：更新文档；移动备份与辅助文件到专用目录；执行构建验证；提交并准备推送。
回滚：将归档文件移回根目录并恢复相关文档描述。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；推送目标仓库地址待确认/未知。

# 2026-03-01 20:34
时间：2026-03-01 20:34
目标：根据用户最新 Tokens 更新 Paper 调色板中的 `orange` 列。
改动：影响面判断已完成；计划仅修改 Paper 画板里 `orange` 对应 step 的展示文本和色块填充，不重建整张调色板。
当前：用户只调整了 `orange` 的中深色阶，现有 Paper 画板需要和最新 Tokens 保持一致。
禁止动：不修改其他 palette，不改布局、不改命名、不补充额外字段；仅同步 `orange` 的变更值。
待办：更新 `orange` 色块文字与背景；截图确认结果。
回滚：恢复 `orange` 原有旧 hex 展示。
约束：文档优先、最小补丁、中文优先；以用户给定 Tokens 为唯一真值。

# 2026-03-01 20:27
时间：2026-03-01 20:27
目标：根据用户最新 Tokens 更新 Paper 调色板中的 `violet` 列。
改动：影响面判断已完成；计划仅修改 Paper 画板里 `violet` 对应 step 的展示文本和色块填充，不重建整张调色板。
当前：用户只调整了 `violet` 的部分深色阶，现有 Paper 画板需要和最新 Tokens 保持一致。
禁止动：不修改其他 palette，不改布局、不改命名、不补充额外字段；仅同步 `violet` 的变更值。
待办：更新 `violet` 色块文字与背景；截图确认结果。
回滚：恢复 `violet` 原有旧 hex 展示。
约束：文档优先、最小补丁、中文优先；以用户给定 Tokens 为唯一真值。

# 2026-03-01 20:18
时间：2026-03-01 20:18
目标：在 Paper 中根据用户提供的 Hue Forge Tokens 原样绘制 14 组、每组 12 阶的调色板。
改动：影响面判断已完成；计划不修改前端代码，仅在 Paper 画布新建一个调色板画板，按给定 palette 名称、step 和 hex 直接渲染。
当前：用户已提供完整的 Tokens 数据，并明确要求在 Paper 中绘制，不需要重新推导颜色或导入到代码工程。
禁止动：不修改现有 React 代码、不改现有导出逻辑、不重命名 palette、不补造缺失色值；仅按给定数据绘制。
待办：创建 Paper artboard；按 palette 分列绘制 swatch；完成后告知画板位置与结果。
回滚：删除新建的 Paper 调色板画板。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；以用户给定 Tokens 为唯一真值。

# 2026-03-01 20:10
时间：2026-03-01 20:10
目标：为头部扩阶增加最浅阶 `saturation` 下限，避免无论扩到多少阶时 `10` 掉成 `0`。
改动：影响面判断已完成；计划仅调整 `useColorSystem` 头部插入时的 `saturation` 目标值计算，在最浅一阶新增 `3%` 最低值约束。
当前：当前最浅阶在持续头部扩阶后可能被去色到 `0`，会让彩色条最浅阶失去必要的颜色识别。
禁止动：不修改亮度扩阶逻辑、不改动中段/尾段插值与现有 hue 模板数组；仅增加最浅阶饱和度下限。
待办：完成 helper 补丁；补充 `ARCHITECTURE.md` / `CHANGELOG.md`；执行类型检查与构建验证。
回滚：恢复当前 `getHeadTargetValue()` 的饱和度目标值实现。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；只加下限约束。

# 2026-03-01 20:02
时间：2026-03-01 20:02
目标：将 `Red` 的饱和度算法调整为用户提供的目标曲线走势。
改动：影响面判断已完成；计划仅修改 `useColorSystem` 中 `RED_SATURATION_SCALE`，使其呈现“前段低起、中段爬升、后段回落”的曲线。
当前：现有 `Red` 饱和度模板与目标图示不一致，尤其前段不够克制、尾段回落节奏不符合预期。
禁止动：不修改其他 hue 模板、不调整亮度曲线与扩阶逻辑；仅替换 `Red` 饱和度模板。
待办：完成模板替换；补充 `ARCHITECTURE.md` / `CHANGELOG.md`；执行类型检查与构建验证。
回滚：恢复当前 `RED_SATURATION_SCALE`。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；只改 `Red` 饱和度。

# 2026-03-01 19:55
时间：2026-03-01 19:55
目标：回退上一轮头部扩阶细修，恢复到更早一版已通过验证的头部扩阶逻辑。
改动：影响面判断已完成；计划撤销刚才新增的头部亮度/饱和度区间权重补偿与单调归一化，仅保留上一版统一头部去色实现。
当前：上一轮细修虽然试图同时解决 `Amber → Blue` 偏深与 `Fuchsia → Red` 偏白，但结果不符合预期，需要回到更稳定的前一版。
禁止动：不修改中段/尾部插值、不改动现有色相模板数组与 UI；仅回退上一轮头部算法调整。
待办：完成代码与文档回退；执行类型检查与构建验证。
回滚：恢复刚才那轮头部亮度/饱和度细修逻辑。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；只回退上一轮。

# 2026-03-01 19:48
时间：2026-03-01 19:48
目标：按结果继续校准头部扩阶，让 `Amber → Blue` 的最浅阶再轻一点，同时让 `Fuchsia → Red` 的最浅阶不再过白。
改动：影响面判断已完成；计划仅调整 `useColorSystem` 中头部插入的亮度/饱和度目标值权重，不引入新的角色层或额外配置。
当前：当前统一头部修正已缓解大部分问题，但 `Amber → Blue` 仍略偏重，`Fuchsia → Red` 则因最浅阶去色过多、提亮过多而显白。
禁止动：不修改中段/尾段插值、不改动现有色相模板数组与 UI 交互；仅继续校准头部最浅阶。
待办：完成权重校准；补充 `ARCHITECTURE.md` / `CHANGELOG.md`；执行类型检查与构建验证。
回滚：恢复当前头部目标值计算权重。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；只调结果。

# 2026-03-01 19:39
时间：2026-03-01 19:39
目标：继续微调头部扩阶的区间化修正，分别处理 `Amber → Blue` 偏深与 `Fuchsia → Red` 偏白问题。
改动：影响面判断已完成；计划在 `useColorSystem` 的头部插入逻辑中加入基于 hue 区间的二次校正：
`Amber → Blue` 进一步压低最浅阶饱和度，
`Fuchsia → Red` 轻微压低头部亮度并回补少量饱和度。
当前：统一的头部去色修正已缓解 `Amber → Blue` 偏深，但还不够；与此同时 `Fuchsia → Red` 在新增最浅阶时会显得偏白。
禁止动：不修改中段/尾部插值、不改动现有色相模板数组与 UI 交互；仅调整头部扩阶的区间化细修。
待办：完成区间化修正；补充 `ARCHITECTURE.md` / `CHANGELOG.md`；执行类型检查与构建验证。
回滚：恢复当前仅按统一头部去色处理的实现。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；只调头部最浅阶。

# 2026-03-01 19:25
时间：2026-03-01 19:25
目标：压低 `24 Hues` 在头部扩阶后 `Amber → Blue` 区间的最浅阶饱和度，缓解新增最浅阶后颜色显得偏深的问题。
改动：影响面判断已完成；计划仅调整 `useColorSystem` 头部插入时的 `saturation` 目标值计算，让高亮区最浅 1-3 阶更积极去色。
当前：当前头部扩阶已拉开亮度，但 `Amber → Blue` 一段在 24 阶下前端仍保留过多饱和度，导致新增最浅阶后视觉上比其他色条更重。
禁止动：不修改亮度扩阶逻辑、不改动中段/尾部插值与现有色相模板数组；仅收紧头部饱和度扩展。
待办：完成头部去色修正；补充 `ARCHITECTURE.md` / `CHANGELOG.md`；执行类型检查与构建验证。
回滚：恢复当前头部 `saturation` 目标值计算方式。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；只调头部饱和度。

# 2026-03-01 19:15
时间：2026-03-01 19:15
目标：移除 `Role Recommender` 功能及其相关文档说明。
改动：影响面判断已完成；计划删除工具栏入口、推荐弹层、推荐算法与相关类型，并回滚 `README.md` / `ARCHITECTURE.md` / `CHANGELOG.md` 中的说明。
当前：当前 recommender 已接入项目，但产品方向已确认不继续保留。
禁止动：不修改现有色板编辑、导出和右侧面板逻辑；仅移除 recommender 相关实现。
待办：完成代码与文档回退；执行类型检查与构建验证。
回滚：恢复 `Role Recommender` 入口、组件、算法与文档说明。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；只做功能移除。

# 2026-03-01 19:08
时间：2026-03-01 19:08
目标：将 `Role Recommender` 从“仅 strip 级推荐”升级为“strip + step + 预览”模式，并修正候选评分。
改动：影响面判断已完成；计划为 recommender 增加 `Primary step` 选择、角色建议 token、组合预览卡，并收紧现有 hue 候选评分，避免推荐结果与用户直觉差距过大。
当前：当前 recommender 只推荐色条，不提供具体色阶建议，也缺少组合预览，导致结果难以判断；同时 chromatic 候选评分过松，会放大远距离色条的误判概率。
禁止动：不改动主色板编辑逻辑与导出逻辑；不新增角色写回；仅增强 recommender 的判断与展示。
待办：补充 `ARCHITECTURE.md` / `README.md` 说明；实现 step-aware 推荐与预览；执行类型检查与构建验证。
回滚：恢复当前 `Role Recommender` 的 strip 级结果卡和评分策略。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；只增强推荐体验。

# 2026-03-01 18:56
时间：2026-03-01 18:56
目标：增加轻量 `Role Recommender` 功能，基于用户指定的主品牌色与搭配算法，从当前现有色条中推荐角色归属。
改动：影响面判断已完成；计划新增角色推荐算法与弹层面板，通过工具栏入口触发；只输出 `Secondary / Accent / Neutral / Accent Neutral` 的推荐结果，不改写当前 palette。
当前：当前工具已支持完整色板编辑，但缺少“在现有色条中如何分配品牌角色”的辅助判断能力。
禁止动：不生成新色条、不新增复杂页面路由、不改动现有配色算法与导出逻辑；仅增加推荐功能与展示界面。
待办：补充 `ARCHITECTURE.md` / `README.md` 说明；实现推荐算法与弹层；执行类型检查与构建验证。
回滚：移除 `Role Recommender` 相关工具栏入口、弹层组件与推荐算法文件。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；只做角色推荐，不做角色写回。

# 2026-03-01 18:41
时间：2026-03-01 18:41
目标：修复左侧 `Base` 轨道的数值来源不一致与弹窗无法关闭问题。
改动：影响面判断已完成；计划统一 `baseScale` 与 `Gray` 列的亮度来源，并为 `BaseScaleRail` 增加外部点击 / `Esc` 关闭逻辑。
当前：当前左侧轨道显示 `baseScale`，而右侧 `Gray` 列使用独立 brightness 曲线，导致数值不一致；节点弹窗缺少关闭路径，只能悬停存在。
禁止动：不修改其他 hue 的模板曲线与 `Base` 轨道拖拽/新增规则；不改动右侧曲线面板；仅修复数值一致性与弹窗关闭交互。
待办：完成 `Gray` 与 `baseScale` 同步；补充弹窗关闭逻辑；执行类型检查与构建验证。
回滚：恢复当前 `Gray` 独立 brightness 曲线与 `BaseScaleRail` 弹窗逻辑。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；只修复当前交互问题。

# 2026-03-01 18:31
时间：2026-03-01 18:31
目标：优化 `Base Scale` 头部新增 step 时的曲线扩展算法，解决新增后 `10/20` 颜色过于接近的问题。
改动：影响面判断已完成；计划仅调整 `useColorSystem` 中 step 插入 helper，把头部插入从简单平均改为边缘重分布，并同步更新架构文档说明。
当前：当前头部插入会在 hue 曲线前端插入几乎重复的 brightness / saturation 值，导致新增最浅阶后 `10` 与 `20` 视觉差异不足。
禁止动：不修改现有色相模板数组与 UI 交互；不改动中段插入逻辑；仅优化头部扩阶的曲线生成方式。
待办：完成头部扩阶算法修正；补充 `ARCHITECTURE.md` / `CHANGELOG.md`；执行类型检查与构建验证。
回滚：恢复当前 `insertInterpolatedValue()` 的头部平均插入实现。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；只调扩阶算法。

# 2026-03-01 18:20
时间：2026-03-01 18:20
目标：删除右侧 `InsightPanel` 中空置且低价值的 `Contrast Accessibility` 区块。
改动：影响面判断已完成；计划仅移除 `InsightPanel` 底部的 `Contrast Accessibility` 渲染块，不调整曲线图、编辑滑杆与数据计算逻辑。
当前：该区块长期为空或信息密度过低，已无法支持有效决策，反而占用右侧面板空间。
禁止动：不修改亮度安全区计算、曲线图配置与右侧面板整体结构；仅删除该低价值区块。
待办：完成区块删除；补充变更记录；执行类型检查与构建验证。
回滚：恢复 `InsightPanel` 中的 `Contrast Accessibility` 区块渲染逻辑。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；只做删除。

# 2026-03-01 18:15
时间：2026-03-01 18:15
目标：将左侧 `Base` 纵向轨道宽度调回 `66px`，移除描边，并让节点内芯颜色与对应灰度完全一致。
改动：影响面判断已完成；计划仅调整 `BaseScaleRail` 的轨道宽度、边框样式和节点内芯颜色，不修改拖拽、新增、删除逻辑。
当前：当前轨道宽度已对齐到 `32px`，但用户希望恢复更宽的 `66px`；轨道描边偏重；节点内芯仍使用固定深色，未反映对应 brightness 的真实灰度。
禁止动：不修改 Base 轨道的交互规则、浮层逻辑与滚动结构；不改动色板算法；仅收敛视觉表现。
待办：完成宽度、描边与节点颜色调整；补充变更记录；执行类型检查与构建验证。
回滚：恢复当前 `BaseScaleRail` 的 `32px` 轨道宽度、描边和固定内芯颜色。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；只改视觉样式。

# 2026-03-01 18:08
时间：2026-03-01 18:08
目标：让左侧 `Base` 纵向轨道的左边界对齐横向 `HueBar` 的起始基准线。
改动：影响面判断已完成；计划仅调整 `BaseScaleRail` 的水平对齐方式，使内轨道从左侧固定槽位起算，而不是居中。
当前：当前 `Base` 轨道宽度已与 `HueBar` 对齐，但仍处于列内居中状态，未与横向色带的起始边界形成同一条纵向基准线。
禁止动：不修改 Base 轨道的拖拽、新增、删除与 brightness 算法；不改动列宽、浮层和滚动结构；仅修正水平对齐。
待办：完成左边界对齐；补充变更记录；执行类型检查与构建验证。
回滚：恢复当前 `BaseScaleRail` 的居中对齐方式。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；只调对齐方式。

# 2026-03-01 18:02
时间：2026-03-01 18:02
目标：将左侧 `Base` 内轨道宽度对齐到横向 `HueBar` 的条高基准。
改动：影响面判断已完成；计划仅调整 `BaseScaleRail` 内轨道宽度，使其与 `HueBar` 当前 `h-8` 的视觉尺寸一致。
当前：当前 `Base` 轨道形态已收平，但内轨道仍偏宽，与横向 `HueBar` 的细条比例不一致。
禁止动：不修改 Base 轨道的拖拽、新增、删除与 brightness 算法；不改动左列滚动结构与浮层逻辑；仅收敛内轨道宽度。
待办：完成宽度对齐；补充变更记录；执行类型检查与构建验证。
回滚：恢复当前 `BaseScaleRail` 内轨道宽度。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；只调轨道宽度。

# 2026-03-01 17:56
时间：2026-03-01 17:56
目标：移除左侧 `Base` 内轨道的大圆角，使其回到页面现有的直线型视觉语言。
改动：影响面判断已完成；计划仅调整 `BaseScaleRail` 内轨道的圆角样式，保留四周槽位和现有交互。
当前：`Base` 轨道虽然已有槽位，但内轨道仍然带明显大圆角，和页面主体的轻量直线风格不一致。
禁止动：不修改 Base 轨道的拖拽、新增、删除与 brightness 算法；不改动左列宽度和滚动结构；仅收敛内轨道形态。
待办：完成内轨道去圆角；补充变更记录；执行类型检查与构建验证。
回滚：恢复当前 `BaseScaleRail` 内轨道的大圆角样式。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；只改轨道圆角。

# 2026-03-01 17:50
时间：2026-03-01 17:50
目标：为左侧 `Base` 轨道增加上下左右槽位，使其与横向 `HueBar` 的“外层槽位 + 内层轨道”结构保持一致。
改动：影响面判断已完成；计划仅调整 `BaseScaleRail` 的容器结构，在左列内容区内留出四周安全槽位，并将灰度渐变收敛为中间内轨道。
当前：当前 `Base` 区域仍是整列直接承载灰度渐变，缺少与横向 `HueBar` 一致的轨道留白与层次。
禁止动：不修改 Base 轨道的拖拽、新增、删除与 brightness 算法；不改动左列滚动行为与其他色板结构；仅统一轨道样式语言。
待办：完成四周槽位与内轨道重构；补充变更记录；执行类型检查与构建验证。
回滚：恢复当前 `BaseScaleRail` 的整列渐变实现。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；只调整轨道层级。

# 2026-03-01 17:44
时间：2026-03-01 17:44
目标：进一步收窄左侧 `Base` 列宽度，并简化标题文案。
改动：影响面判断已完成；计划仅调整 `MatrixBoard` 中左侧固定列的宽度与标题文本，从 `Base Scale` 收敛为 `Base`。
当前：左侧固定列在拆分滚动后行为已正确，但视觉上仍偏宽，标题层级也略显冗长。
禁止动：不修改 Base 轨道交互、色板滚动结构与右侧面板；不调整 brightness 算法；仅收敛左列比例与文案。
待办：完成列宽与标题调整；补充变更记录；执行类型检查与构建验证。
回滚：恢复当前左侧列宽度与 `Base Scale` 标题。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；只调比例与文案。

# 2026-03-01 17:38
时间：2026-03-01 17:38
目标：将 `MatrixBoard` 的滚动职责拆分为“左侧 Base Scale 固定、中间色板区独立滚动”。
改动：影响面判断已完成；计划调整 `MatrixBoard` 容器结构，移除左侧 `Base Scale` 对统一滚动容器的依赖，仅保留中间色板区作为滚动层。
当前：当前 `MatrixBoard` 仍使用单一滚动容器，导致左侧 `Base Scale` 会随中间内容一起参与垂直滚动，行为不符合界面预期。
禁止动：不修改颜色算法、Base Scale 拖拽逻辑与右侧 `InsightPanel`；不改动色板卡片内容；仅重构滚动容器层级。
待办：完成左右分层滚动；补充变更记录；执行类型检查与构建验证。
回滚：恢复当前 `MatrixBoard` 的单一滚动容器实现。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；只修正滚动行为。

# 2026-03-01 17:31
时间：2026-03-01 17:31
目标：让 `Base Scale` 灰度渐变填充整个左侧内容容器，并移除导致其视觉长度独立于主布局的额外补偿。
改动：影响面判断已完成；计划仅调整 `BaseScaleRail` 的容器层级，将渐变背景提升到左侧内容区本身，并删除底部额外留白。
当前：当前 `Base Scale` 仍表现为容器中的独立轨道卡片，未填充整列；同时内部补偿会让这块区域的长度节奏独立于主布局。
禁止动：不修改 Base Scale 的拖拽、新增、删除与 brightness 计算逻辑；不改动右侧面板与色板算法；仅调整容器呈现方式。
待办：完成容器填充与高度节奏收敛；补充变更记录；执行类型检查与构建验证。
回滚：恢复当前 `BaseScaleRail` 的独立轨道容器和底部留白实现。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；只改布局表现。

# 2026-03-01 17:24
时间：2026-03-01 17:24
目标：收窄 `Base Scale` 轨道宽度、缩小圆角，并将其长度节奏对齐右侧色板列。
改动：影响面判断已完成；计划仅调整 `BaseScaleRail` 的内层宽度、上下留白和圆角尺度，使其与右侧色板内容区在视觉节奏上保持一致。
当前：当前轨道虽然已改为纯灰度，但本体仍显得过宽、圆角偏大，且底部长度比右侧色板列更满。
禁止动：不修改 Base Scale 的拖拽、新增、删除与 brightness 计算逻辑；不改动右侧色板内容与整体布局结构；仅收敛轨道尺寸。
待办：完成宽度、圆角和长度对齐；补充变更记录；执行类型检查与构建验证。
回滚：恢复当前 `BaseScaleRail` 的宽度、圆角和高度表现。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；只调整视觉比例。

# 2026-03-01 17:18
时间：2026-03-01 17:18
目标：将 `Base Scale` 轨道调整为纯灰度渐变，并统一该区域的圆角语言。
改动：影响面判断已完成；计划仅调整 `MatrixBoard` 中 `BaseScaleRail` 的轨道渐变、边框与圆角层级，不改动交互逻辑与 brightness 数据。
当前：当前轨道渐变带有偏蓝灰倾向，且使用了独立的大圆角值，与页面现有卡片和面板的圆角体系不一致。
禁止动：不修改 Base Scale 的拖拽、新增、删除逻辑；不改动色板计算与其他组件布局；仅收敛视觉样式。
待办：完成纯灰度与圆角统一；补充变更记录；执行类型检查与构建验证。
回滚：恢复当前 `BaseScaleRail` 的渐变与圆角参数。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；只调整视觉语言。

# 2026-03-01 17:12
时间：2026-03-01 17:12
目标：移除 `Base Scale` 轨道底部多余的英文提示文案。
改动：影响面判断已完成；计划仅删除 `BaseScaleRail` 中的 “Click track to add a brightness point” 提示，不调整交互逻辑。
当前：轨道交互已经具备，底部提示文案冗余且破坏界面简洁性。
禁止动：不修改 Base Scale 轨道行为、颜色算法与其他布局结构；仅清理该提示文本。
待办：完成文案移除；执行类型检查验证。
回滚：恢复轨道底部提示文案。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；仅删文案。

# 2026-03-01 17:07
时间：2026-03-01 17:07
目标：将 `Base Scale` 从卡片列重构为纵向灰阶轨道控件，支持圆点拖拽与点击新增。
改动：影响面判断已完成；计划改造 `MatrixBoard` 的 `Base Scale` 区域，并扩展 `useColorSystem` 支持按 brightness 插入 step 的新逻辑。
当前：当前 `Base Scale` 仍是卡片列表，交互模型与 Hue 轨道不一致；新增方式也不符合轨道式编辑预期。
禁止动：不修改颜色计算公式与右侧曲线面板；保持 Base Scale 与各 hue 曲线长度同步。
待办：完成轨道控件实现；补充变更记录；执行类型检查与构建验证。
回滚：恢复当前 `Base Scale` 卡片列 UI 与 `addStep()` 的头尾新增实现。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；新交互需保持 brightness 单调递减。

# 2026-03-01 17:00
时间：2026-03-01 17:00
目标：修正 HueBar 两端未真正留出视觉空隙的问题。
改动：影响面判断已完成；计划仅调整 `HueBar` 色带本体宽度计算方式，使左右安全区既作用于交互，也真实作用于视觉层。
当前：HueBar 已有左右安全区坐标逻辑，但渐变条本体仍使用 `w-full + margin`，视觉上依然贴边。
禁止动：不修改 hue 拖拽规则、矩阵布局与颜色算法；仅修正色带本体尺寸实现。
待办：完成色带宽度修正；执行类型检查验证。
回滚：恢复当前渐变条的 `w-full + margin` 实现。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；只修正左右留白表现。

# 2026-03-01 16:57
时间：2026-03-01 16:57
目标：缩小 `Auto-Generate` 分段控件尺寸，使其与页面整体比例更协调。
改动：影响面判断已完成；计划仅下调 `Toolbar` 中标签字号、按钮内边距和最小宽度，保持当前 ChatGPT 风格不变。
当前：当前分段控件风格已收敛，但整体尺寸仍偏大，与页面其余控件不够协调。
禁止动：不修改色板生成逻辑、不改变导出按钮和工具栏结构；仅调整该控件尺度。
待办：完成尺寸收缩；执行类型检查验证。
回滚：恢复当前 `Toolbar` 分段控件尺寸参数。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；仅改视觉尺度。

# 2026-03-01 16:54
时间：2026-03-01 16:54
目标：将 `Auto-Generate` 分段控件从拟物风格调整为更接近 ChatGPT 的简洁风格。
改动：影响面判断已完成；计划仅调整 `Toolbar` 中分段控件的样式，去掉厚重拟物阴影，改为浅灰容器、白色选中态和轻描边。
当前：当前控件拟物感过重，视觉风格与页面其余区域不统一；用户明确要求改为 ChatGPT 风格。
禁止动：不修改色板生成逻辑与工具栏其他功能；不调整导出面板或颜色算法。
待办：完成控件样式回调；执行类型检查与构建验证。
回滚：恢复上一版轻拟物样式实现。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；仅改该控件视觉样式。

# 2026-03-01 16:50
时间：2026-03-01 16:50
目标：将 `Auto-Generate` 分段控件调整为轻拟物风格。
改动：影响面判断已完成；计划仅修改 `Toolbar` 中 `8/12/24 Hues` 控件的样式，并增加当前点击项的轻拟物选中状态。
当前：当前分段控件为普通浅灰按钮组，层次偏平；不符合目标中的轻质感拟物风格。
禁止动：不修改色板生成逻辑与其他工具栏结构；不改导出面板与颜色算法。
待办：完成控件样式调整；执行类型检查验证。
回滚：恢复 `Toolbar` 中原始按钮组样式与无选中状态实现。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；仅改该控件视觉表现。

# 2026-03-01 16:45
时间：2026-03-01 16:45
目标：将 HueBar 调整为带上下槽位和左右安全区的轨道式控件。
改动：影响面判断已完成；计划重构 `HueBar` 的布局结构与位置计算：`Accent Gray` 走上槽位，其他色相走下槽位，并为轨道增加左右安全区。
当前：当前 HueBar 虽已通栏，但手柄仍直接贴近色带边缘，左右端点与多手柄密集时的可读性不足。
禁止动：不修改 hue 拖拽规则、颜色算法与矩阵逻辑；仅调整轨道结构、手柄布局与坐标映射。
待办：完成控件结构改造；执行类型检查与构建验证。
回滚：恢复当前单层色带结构与旧的 hue 映射计算方式。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；需保持现有交互语义一致。

# 2026-03-01 16:39
时间：2026-03-01 16:39
目标：将 Hue 色带区域调整为无间距、通栏展示。
改动：影响面判断已完成；计划移除 `Hue Spectrum Input` 外层 padding，并调整 `HueBar` 的渐变条样式为贴边通栏。
当前：当前色带区域外层存在 `px-8 py-4` 留白，渐变条本身也有圆角与阴影，不符合通栏预期。
禁止动：不修改 hue 拖拽逻辑、颜色算法与手柄交互规则；仅调整该区域布局与视觉样式。
待办：完成通栏样式调整；执行类型检查验证。
回滚：恢复 `App` 中色带容器 padding 与 `HueBar` 的圆角/阴影样式。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；只改该区域视觉结构。

# 2026-03-01 16:35
时间：2026-03-01 16:35
目标：在导出面板中增加 `AI Export` 开关，使复制内容可切换为适合直接提供给 AI 的文本。
改动：影响面判断已完成；计划扩展导出生成函数，增加 AI 友好包装文本，并在 `ExportPanel` 中增加开关与复制状态联动。
当前：当前导出面板虽然包含 `Tokens` 等结构化格式，但复制内容默认仍是原始代码/数据，不区分开发导出与 AI 导出。
禁止动：不修改颜色算法、不改变现有四种导出格式的原始内容；仅增加 AI 包装层与 UI 开关。
待办：完成导出开关实现；同步文档并执行 lint/build 验证。
回滚：移除 `AI Export` 开关与导出包装函数，恢复当前导出面板原状。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；AI 导出需保持原始颜色数据不变。

# 2026-03-01 16:29
时间：2026-03-01 16:29
目标：参考 `kigen.design/color` 增加多格式颜色导出能力，并让导出内容可直接提供给 AI 使用。
改动：影响面判断已完成；计划用 `CSS / Tailwind / Tailwind 4 / Tokens` 四种格式替换现有单一 `Export SVG` 入口，新增导出生成函数与面板 UI。
当前：当前项目仅支持导出 SVG 字符串；没有代码导出、Token 结构化输出，也缺少适合 AI 直接消费的纯文本格式。
禁止动：不修改颜色算法核心、不改主矩阵交互逻辑；导出功能优先复用现有 palette 数据，不引入后端依赖。
待办：实现导出生成逻辑；接入工具栏面板；更新文档并执行 lint/build 验证。
回滚：恢复工具栏 `Export SVG` 按钮与原始 `handleCopySvg` 实现，删除新增导出面板与工具函数。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；导出文本需兼顾开发使用与 AI 可读性。

# 2026-03-01 16:24
时间：2026-03-01 16:24
目标：将导航 Logo 调整为 Lime 色系，并移除右侧 SVG 操作入口。
改动：影响面判断已完成；计划修改 `TopBar` 的 Logo 渐变与字标样式，删除“从SVG拷贝到Figma”“下载SVG”按钮，并清理无用传参。
当前：当前 Logo 为蓝青色渐变，右侧仍保留两个 SVG 操作按钮；这些入口当前不符合新的导航风格要求。
禁止动：不修改颜色算法、主内容布局与其他交互；不新增外部图片资源。
待办：完成顶栏样式与结构调整；执行类型检查验证。
回滚：恢复 `TopBar` 之前的蓝青 Logo 与右侧按钮实现。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；Logo 继续使用内联实现。

# 2026-03-01 16:20
时间：2026-03-01 16:20
目标：将顶部导航品牌改为产品名 `Hue Forge`，并增加 Logo。
改动：影响面判断已完成；计划仅修改 `TopBar` 的品牌展示区域，加入内联 SVG Logo，并同步更新变更记录。
当前：顶栏左侧仍显示“设计系统”，没有独立品牌 Logo；项目对外名称已经统一为 `Hue Forge`。
禁止动：不修改颜色算法、页面主体布局与其他交互逻辑；不新增外部图片依赖。
待办：完成导航品牌替换；执行类型检查验证。
回滚：恢复 `TopBar` 原始标题与无 Logo 状态。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；Logo 使用内联实现以避免额外资源管理。

# 2026-03-01 16:16
时间：2026-03-01 16:16
目标：按校准反馈修正 `60/70` 对比度问题，并优化扩展到 `12阶` 时顶部 step 过白的问题。
改动：影响面判断已完成；计划下调 `Accent Gray`、`Lime`、`Cyan`、`Blue`、`Indigo` 的中段亮度曲线，微调 `Cyan/Blue` hue 区间边界，并重写新增 step 的头部扩展算法。
当前：`12/24 Hues` 中部分 `60/70` step 未稳定满足预期可读性；扩展到 `12阶` 时顶部新 step 因亮度过高、饱和度过低而偏白。
禁止动：不修改整体 UI 结构、不改项目命名、不重构颜色空间；本轮仅做曲线与扩展规则微调。
待办：完成代码补丁；更新 `ARCHITECTURE.md` / `CHANGELOG.md`；执行类型检查与构建验证。
回滚：恢复相关 brightness 曲线、hue 区间边界和 `addEdgeValues` 旧实现。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；以当前实现和本地对比度计算结果为准。

# 2026-03-01 16:07
时间：2026-03-01 16:07
目标：在 `ARCHITECTURE.md` 中补充每个颜色角度区间的算法核心说明。
改动：影响面判断已完成；计划细化 hue 区间到曲线模板的映射规则，补充每个颜色角度的饱和度/亮度策略与特殊说明。
当前：文档已描述总体算法思路，但对各 hue 区间仍不够细，无法直接作为颜色规则索引使用。
禁止动：不修改任何颜色算法实现、不调整 UI、不改 README/API；仅更新架构文档。
待办：完成 `ARCHITECTURE.md` 颜色区间规则补丁。
回滚：删除本次新增的区间规则说明段落即可。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；内容需与当前实现严格一致。

# 2026-03-01 16:05
时间：2026-03-01 16:05
目标：将当前颜色生成算法总结补充到 `ARCHITECTURE.md`。
改动：影响面判断已完成；计划基于现有 `useColorSystem` 与 `color.ts` 逻辑，对架构文档补充算法流程、规则映射与关键设计决策。
当前：颜色算法已存在于代码中，但架构文档只描述了模块职责，缺少可直接对应实现的算法摘要。
禁止动：不修改颜色算法代码、不调整 UI 行为、不改 README/API；仅更新架构文档。
待办：完成 `ARCHITECTURE.md` 最小补丁。
回滚：删除本次新增的算法摘要段落，恢复文档原状。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；文档内容需与当前实现一致。

# 2026-03-01 16:03
时间：2026-03-01 16:03
目标：调整 `Accent Gray` 的命名行为，使拖动 hue 手柄时不自动改名。
改动：影响面判断已完成；计划仅修改 `Accent Gray` 初始配置的命名标记，保持其在 hue 变更时固定显示名称。
当前：当前所有未标记 `customName` 的色相在 hue 变更时都会自动改名；`Accent Gray` 也受此逻辑影响。
禁止动：不修改其他色相的自动命名规则；不调整拖拽逻辑、曲线算法与 UI 布局。
待办：完成命名行为修正；执行类型检查验证。
回滚：移除 `Accent Gray` 的固定命名标记，恢复随 hue 自动改名。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；仅修正名称行为。

# 2026-03-01 16:01
时间：2026-03-01 16:01
目标：将 `Accent Gray` 的色相手柄改为显示在色带上方，与彩色手柄方向区分。
改动：影响面判断已完成；计划仅调整 `HueBar` 中 `accent-gray` 手柄的定位样式，并补充变更记录。
当前：`Accent Gray` 与其他色相手柄均位于色带下沿，视觉区分不明显；拖拽逻辑共用同一套实现。
禁止动：不修改手柄拖拽约束、颜色算法与其他组件结构；不回退既有 `Accent Gray` 曲线优化。
待办：完成手柄定位调整；执行类型检查验证。
回滚：恢复 `accent-gray` 手柄与其他手柄一致的下方定位样式。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；仅改视觉朝向，不改交互语义。

# 2026-03-01 15:59
时间：2026-03-01 15:59
目标：移除右侧面板中效果不明显的 `Batch Adjust` 批量调节入口。
改动：影响面判断已完成；计划删除 `InsightPanel` 中的批量调节 UI，并清理 `App` 与 `useColorSystem` 中对应的未使用逻辑。
当前：`Batch Adjust` 已上线但效果不明显；相关影响范围限于右侧面板整列调节功能，不涉及默认 `Accent Gray` 曲线。
禁止动：不回退 `Accent Gray` 默认曲线优化；不修改单 step 调节逻辑、项目命名与 Base Scale 行为。
待办：完成 UI 与逻辑清理；执行类型检查与构建验证。
回滚：恢复 `InsightPanel` 批量调节区域与 `scaleCurve` 调用链。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；避免保留无调用死代码。

# 2026-03-01 15:56
时间：2026-03-01 15:56
目标：优化 `Accent Gray` 默认算法，并在右侧面板增加整列批量调整能力。
改动：影响面判断已完成；计划下调 `Accent Gray` 饱和度曲线，扩展 `InsightPanel` 与 `useColorSystem`，支持对选中色板整列执行批量饱和度调整。
当前：`Accent Gray` 当前饱和度整体偏高；右侧面板仅支持单 step 调节，缺少整列批量操作入口。
禁止动：不修改其他色相的默认曲线；不改颜色空间实现；不变更 Base Scale 逻辑与项目名称。
待办：完成默认曲线优化；增加批量调节控件；执行类型检查与构建验证。
回滚：恢复 `ACCENT_GRAY_SATURATION_SCALE` 原值，并移除整列批量调节入口与对应 hook 方法。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；整列调整需保持曲线长度与取值范围合法。

# 2026-03-01 15:53
时间：2026-03-01 15:53
目标：调整 `Base Scale` 新增逻辑，使每次新增时同时在头部和尾部各增加一个 step。
改动：影响面判断已完成；计划修改 `useColorSystem` 的 step 增长逻辑及 `MatrixBoard` 的新增按钮调用方式，并补充变更记录。
当前：当前实现为单次点击仅在尾部插入一个 step；相关影响范围限于 Base Scale 与各色相曲线同步扩展。
禁止动：不修改颜色计算公式、删除逻辑与其他组件结构；不改动项目命名和备份文件。
待办：完成双端新增逻辑；执行类型检查验证。
回滚：恢复 `addStep(index)` 的单点插入实现，并将按钮调用恢复为传入末尾索引。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；保持现有 Base Scale 与色相曲线长度同步。

# 2026-03-01 15:50
时间：2026-03-01 15:50
目标：将项目名称更新为 `Hue Forge`。
改动：影响面判断已完成；计划同步更新 `package.json`、`index.html`、`README.md`、`CHANGELOG.md` 中的项目名称标识。
当前：项目运行正常；当前根目录包名为 `color-tools`，页面标题为 `Color Tools`，对外名称尚未统一。
禁止动：不修改颜色生成逻辑、组件结构与依赖版本；不处理压缩备份文件。
待办：完成名称统一并验证根目录构建仍通过。
回滚：将名称字段恢复为 `color-tools` / `Color Tools` 即可回退。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；npm 包名使用 URL 友好的小写连字符形式。

# 2026-03-01 15:47
时间：2026-03-01 15:47
目标：清理已完成迁移的历史目录 `color-system-v2.0/`，收敛项目结构。
改动：影响面判断已完成；计划删除历史源码目录，并同步更新 `ARCHITECTURE.md`、`README.md`、`CHANGELOG.md` 对目录状态的描述。
当前：根目录工程可独立运行；待清理对象仅为迁移后冗余目录 `color-system-v2.0/`；压缩包 `color-system-v2.0.zip` 是否保留暂按备份处理。
禁止动：不删除根目录现行源码、依赖与运行配置；不改业务逻辑；不处理用途待确认/未知的压缩备份文件。
待办：删除历史目录并验证根目录结构；如后续确认无需备份，再决定是否清理 zip 包。
回滚：若删除后发现遗漏，可从保留的 `color-system-v2.0.zip` 重新解压恢复。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；本次清理以目录收敛为限。

# 2026-03-01 15:42
时间：2026-03-01 15:42
目标：将 `color-system-v2.0` 的前端代码迁移到仓库根目录，并在根目录启动本地服务。
改动：影响面判断已完成；计划迁移 `src/`、`index.html`、`vite.config.ts`、`tsconfig.json`、`package.json`、`.gitignore`、`.env.example`，并按需补充根目录文档。
当前：根目录已具备可运行前端工程；`npm install`、`npm run lint`、`npm run build` 已通过，`npm run dev` 已在根目录启动并返回 `http://localhost:3000/`；原始代码仍保留在 `color-system-v2.0/` 作为回滚基线。
禁止动：未发现显式 Protected 文件；不修改未确认用途的数据文件与外部服务配置；不删除原目录前先验证根目录可启动。
待办：如需彻底收敛目录，可在确认无误后删除 `color-system-v2.0/` 历史目录；国际化与 Token 规范化待确认/未知。
回滚：删除根目录新增前端文件并保留 `color-system-v2.0/` 原始目录，恢复以子目录运行。
约束：文档优先、最小补丁、中文优先；未知信息必须标注“待确认/未知”；启动结果以本机实际命令验证为准。

# YYYY-MM-DD HH:MM
（最新在上，按时间倒序）
时间：
目标：
改动：
当前：
禁止动：
待办：
回滚：
约束：
